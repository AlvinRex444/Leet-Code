class Solution {
    public void reverseString(char[] s) 
    {
        int ln=s.length;
       int l=0,r=ln-1;
       char t=' ';
       while(l<r)
       {
         t=s[l];
         s[l]=s[r];
         s[r]=t;
         l++;
         r--;
       } 
    }
}
